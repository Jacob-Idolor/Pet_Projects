# Minimal Terraform example

Safe local-only stack — no cloud account required.

```bash
terraform init
terraform validate
terraform plan
terraform apply
terraform destroy
```

Creates `hello.txt` via the `local` provider.
