# Networking module example

Demonstrates module composition with a reusable VPC module.

```bash
terraform init -backend=false
terraform validate
terraform plan
```

Uses `plan` only unless you intend to create real AWS VPC resources.
