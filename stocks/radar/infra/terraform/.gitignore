.terraform/
*.tfstate
*.tfstate.*
# Provider selections are reproducibility metadata and should be committed.
!.terraform.lock.hcl
terraform.tfvars
*.tfvars
!terraform.tfvars.example
crash.log
override.tf
override.tf.json

# Never commit plans (can contain PII / account IDs)
tfplan
*.tfplan
